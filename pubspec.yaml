name: try_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dropdown_search: ^5.0.6
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  firebase_core: ^3.13.1
  cloud_firestore: ^5.6.8
  http: ^0.13.6  # or latest version


dev_dependencies:
  flutter_test:
    sdk: flutter




flutter:
  uses-material-design: true
  generate: true  # ✅ This is required!


